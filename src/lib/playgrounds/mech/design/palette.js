// Limited material palette — UNITY through constraint. Each hue owns a role:
//   frame = structure, plate = armor, joint = mechanism, accent = focal point,
//   glass = cockpit/sensor (emissive). Design engine only ever assigns these.
export const PALETTE = {
  steel: "#8b94a6",  // primary plating
  dark: "#3c4250",   // recessed frame / inner structure
  accent: "#d6552f", // warning orange — the eye-catch
  brass: "#c79a3c",  // joints / pistons
  glass: "#67d3ff",  // cockpit / visor (emissive)
  gun: "#23262e",    // weapon metal
};
